#include "fractions.h"
#include "ibm-utils.h"

extern coord vc;
extern scalar vof;
extern int maxlevel;

#define(a,b) sqrt((sq(a) + sq(b))

vector forceCell[];
vector forceMarker[];
vector forceDesired[];
vector markerCoord[];


